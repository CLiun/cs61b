# cs61b
cs61b-21-spring的课程代码，可使用公开的autograder
## project1
- 构造函数要`public`声明
- Java取模运算根据被除数符号返回正负，用`Math.floorMod(x, y)`替代，或者tricky的运算方式`((negA % B) + B) % B`.

## lab2setup
在IDEA-plugins中下载CS61B插件时，需要到插件homepage找到兼容21年课程版本手动安装。
